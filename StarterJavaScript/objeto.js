// Object
//Criação de objeto
const person = {
    name: 'Agatha',
    age: 19,
    weight: 65.9,
    isAdmin: true
}

console.log(person.name) //como pegar somente um propriedade do Object
console.log(`${person.name} tem ${person.age} anos`) // utilizando a interpolação